{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup": "yarn",
    "dev": "vite --host",
    "build": "vite build & yarn cpx -C client/dist/**/* staging",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.1.0"
  }
}